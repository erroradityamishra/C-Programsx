#include <stdio.h>

void update(int *a,int *b) {
    int av=*a;
    int bv=*b;
    *a=av+bv;
    *b=bv-av;
    // Complete this function    
}

int main() {
    int a, b;
    int *pa = &a, *pb = &b;
    
    scanf("%d %d", &a, &b);
    update(pa, pb);
    printf("%d\n%d", a, b);

    return 0;
}
